name: tkk_token_app
description: Volunteer and admin flow for the TKK token redemption event.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.1.0
  mobile_scanner: ^5.0.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
